# For more info about lambda function read its key points(pg202)

sum = lambda x, y: x + y # As we can see it's a return function
print("Sum =",sum(3,5))

def sum(x,y): # This will do the same as above
    return x+y
print("Sum =",sum(3,5))
